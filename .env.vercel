# Vercel-specific environment variables
# Add these to your Vercel project settings

# Required for Socket.IO to work properly on Vercel
NEXT_PUBLIC_VERCEL=1

# Force polling transport
SOCKET_IO_FORCE_POLLING=true

# Your domain configuration
NEXT_PUBLIC_SITE_URL=https://atlasdebate.com
ALLOWED_ORIGINS=https://atlasdebate.com,https://www.atlasdebate.com

# Optional: Debug mode for troubleshooting
# DEBUG_API_KEY=your-secure-debug-key