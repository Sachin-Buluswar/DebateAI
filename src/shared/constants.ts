export const MAX_UPLOAD_SIZE_BYTES = 25 * 1024 * 1024; // 25 MB
export const MAX_USER_STORAGE_BYTES = 250 * 1024 * 1024; // 250 MB per user
export const UPLOAD_CHUNK_SIZE_BYTES = 2 * 1024 * 1024; // 2 MB chunk size for large uploads
export const MAX_RECORDING_MINUTES = 10; // max recording length for in-browser recorder

// You can tweak these values centrally as product requirements evolve. 